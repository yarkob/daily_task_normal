'use strict';

/**
 * @param {Object[]} addresses
 *
 * @returns {Object[]}
 */
function countAddresses(addresses) {
  return addresses.reduce((acc, obj) => {

  }, []);
}

module.exports = {
  countAddresses,
};
