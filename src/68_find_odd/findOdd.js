'use strict';

/**
 * @param {number[]} numbers
 *
 * @returns {number}
 */
function findOdd(numbers) {
  // const obj = numbers.reduce((acc, num) => {
  //   if (!acc[num]) {
  //     acc[num] = [];
  //   }
  //
  //   acc[num].push(num);
  // }, {});
}

module.exports = {
  findOdd,
};
