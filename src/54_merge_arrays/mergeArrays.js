'use strict';

/**
 * @param {string[]} letters
 * @param {number[]} numbers
 *
 * @returns {string[]}
 */
function mergeArrays(letters, numbers) {
  // write code here
}

module.exports = {
  mergeArrays,
};
