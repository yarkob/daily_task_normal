'use strict';

/**
 * @typedef {Object} Animal
 * @property {string} name
 * @property {number} numberOfLegs
 *
 * @param {Animal[] | null} animals
 *
 * @returns {Animal[] | null}
 */
function sortAnimals(animals) {}

module.exports = {
  sortAnimals,
};
